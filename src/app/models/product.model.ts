export interface Product {
    _id: String,
    name: String,
    price: Number,
    discount: Number,
    size: String,
    sides: Number,
    quantity: number,
    image: string,
    vendor: string
}